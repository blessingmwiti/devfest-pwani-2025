# GCP Configuration
GCP_PROJECT_ID=your-project-id
GCP_REGION=us-central1

# For local development with mock data
USE_MOCK_DATA=true

# Vertex AI Configuration (automatically uses project from GCP_PROJECT_ID)
GOOGLE_GENAI_API_KEY=your-api-key-if-using-googleai-instead-of-vertex

# Server Configuration
PORT=3400
NODE_ENV=development

